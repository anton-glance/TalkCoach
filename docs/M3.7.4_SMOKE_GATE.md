# M3.7.4 — Architecture Z Smoke Gate (14 steps)

**Branch:** `spike/13.5-hal-stop-settling`
**Date executed:** 2026-05-21
**Tester:** Anton Glance
**Build:** (fill in after build)

---

## Exit criteria

All 14 steps PASS → Architecture Z ships. Any FAIL → escalate per step notes.

---

## Step 0 — Baseline test count

Expected: 381 tests (360 current + 21 deleted in sub-commit 4).
Actual: _____
Pass / Fail: _____

---

## Step 1 — Build succeeds

    xcodebuild -scheme TalkCoach -destination 'platform=macOS' build

Expected: `** BUILD SUCCEEDED **`
Actual: _____
Pass / Fail: _____

---

## Step 2 — All unit tests green

    xcodebuild -scheme TalkCoach -destination 'platform=macOS' test

Expected: 0 failures
Actual: _____
Pass / Fail: _____

---

## Step 3 — Menu bar icon appears on launch

Launch the built .app from Finder or build products directory.

Expected: TalkCoach menu bar icon appears within 3 seconds.
Actual: _____
Pass / Fail: _____

---

## Step 4 — Widget appears on mic activation (Voice Memos)

Action: Open Voice Memos, click Record, speak "test one two three."

Expected:
- Widget appears immediately in `.warming` state (spinner visible) as soon as mic activates.
- Widget transitions to `.counting` within ~500ms of engine ready.
- WPM counter begins updating.

Actual: _____
Pass / Fail: _____
First-token latency (ms): _____

---

## Step 5 — Widget linger on session end

Action: Stop Voice Memos recording.

Expected:
- Widget enters linger sequence (`.lingerFull` → fade-out) rather than hiding instantly.
- Widget fully hidden within ~5 seconds of mic deactivation.

Actual: _____
Pass / Fail: _____

---

## Step 6 — `voice-active:` log marker emitted

Action: Run a Voice Memos session and capture `log stream` output.

    log stream --predicate 'subsystem == "com.talkcoach.app" AND category == "session"' --level info

Expected: Lines matching `voice-active: <ISO8601> true reason=voice-onset` and `voice-active: <ISO8601> false reason=debounced-silence` appear during speech and silence respectively.

Actual lines observed: _____
Pass / Fail: _____

---

## Step 7 — VAD debounce: widget enters `.waiting` only after 200ms silence

Action: Speak, pause for < 200ms, resume. Observe widget state.

Expected: Widget does NOT enter `.waiting` on sub-200ms pauses (breath pauses, word gaps).

Actual: _____
Pass / Fail: _____

---

## Step 8 — VAD debounce: widget enters `.waiting` after sustained silence

Action: Speak, then pause for > 300ms. Observe widget state.

Expected: Widget transitions to `.waiting` (isVoiceInactive = true) approximately 200ms after speech ends.

Actual: _____
Pass / Fail: _____

---

## Step 9 — X-button dismiss: widget does not reshow on stale signals

Action: Start Voice Memos, let widget appear, click X-button to dismiss.

Expected:
- Widget dismisses immediately on confirmed X-button tap.
- Widget does NOT reappear on subsequent token or VAD signals.

Actual: _____
Pass / Fail: _____

---

## Step 10 — New session clears prior dismiss

Action: After X-button dismiss (Step 9), stop Voice Memos. Start a new Voice Memos recording.

Expected: Widget reappears in `.warming` on new mic activation (dismissed state cleared for new session).

Actual: _____
Pass / Fail: _____

---

## Step 11 — Coexistence with Zoom (no mic dropout)

Action: Join a Zoom call, start speaking. Open Voice Memos while in the Zoom call.

Expected:
- TalkCoach widget appears and counts speech.
- Zoom audio is unaffected (no dropout, no echo, no degradation).
- No crash or log error.

Actual: _____
Pass / Fail: _____

---

## Step 12 — Session recovery after audio engine config change

Action: While a Voice Memos session is active, connect or disconnect a USB audio device.

Expected:
- Widget enters `.recovering` briefly if engine restarts.
- Widget returns to active state (`.counting` or `.waiting`) within ~2 seconds.
- No crash.

Actual: _____
Pass / Fail: _____

---

## Step 13 — `state-timeline.py` CSV output (widget-state lines)

Action: After a Voice Memos session, pivot the log output:

    python3 tools/state-timeline.py <log-file.txt> > timeline.csv

Expected: CSV contains columns for idle, warming, counting, waiting, wrapping, recovering, dismissed with timestamps.

Actual: _____
Pass / Fail: _____

---

## Step 14 — First-token latency gate

Measured first-token latency from Step 4: _____ ms

Gate:
- ≤ 453ms → PASS (Architecture Z ships)
- > 453ms → FAIL (escalate to Sherpa-ONNX or further relaxation)

Pass / Fail: _____

---

## Summary

| Step | Description | Result |
|------|-------------|--------|
| 0 | Baseline test count | |
| 1 | Build succeeds | |
| 2 | All tests green | |
| 3 | Menu bar icon | |
| 4 | Widget warming→counting | |
| 5 | Widget linger | |
| 6 | voice-active: log marker | |
| 7 | VAD debounce sub-200ms | |
| 8 | VAD debounce 200ms+ | |
| 9 | X-button no reshow | |
| 10 | New session clears dismiss | |
| 11 | Zoom coexistence | |
| 12 | Audio config change recovery | |
| 13 | state-timeline.py CSV | |
| 14 | First-token latency gate | |

**Overall: PASS / FAIL**

Notes: _____
